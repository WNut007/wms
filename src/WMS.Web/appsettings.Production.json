{
  "_": [
    "Phase 26 — Production overrides. Connection strings + secrets MUST come",
    "from environment variables, not from this file. Empty placeholders here",
    "trigger ConfigurationValidator at startup if the env vars aren't set.",
    "",
    "Env var conventions (ASP.NET Core's standard mapping for nested keys):",
    "  ConnectionStrings__MasterDb       — master DB connection string",
    "  ConnectionStrings__TenantTemplate — template used by ITenantConnectionFactory",
    "  Storage__Local__RootPath          — absolute disk path for document storage",
    "",
    "Set via Windows IIS: Site → Configuration Editor →",
    "  system.webServer/aspNetCore/environmentVariables",
    "or via service-account-level env vars; never commit secrets here."
  ],
  "ConnectionStrings": {
    "MasterDb": "",
    "TenantTemplate": ""
  },
  "Storage": {
    "Provider": "Local",
    "Local": {
      "RootPath": "App_Data/storage"
    },
    "MaxFileSizeMB": 50,
    "AllowedExtensions": [
      ".pdf", ".docx", ".doc", ".xlsx", ".xls", ".csv", ".txt",
      ".png", ".jpg", ".jpeg", ".webp", ".gif",
      ".webm", ".mp4"
    ]
  },
  "Email": {
    "_comment": "Phase 30A — production overrides. TestMode MUST be false; Username / Password / FromAddress MUST come from env vars (Email__Username / Email__Password / Email__FromAddress). Empty values here cause EmailServiceHealthCheck to report Unhealthy on /healthz/ready until env vars are wired.",
    "TestMode": false,
    "Username": "",
    "Password": "",
    "FromAddress": ""
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.AspNetCore.HttpsPolicy": "Information",
        "System": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    },
    "WriteTo": [
      {
        "Name": "Console",
        "Args": {
          "outputTemplate": "[{Timestamp:yyyy-MM-dd HH:mm:ss.fff zzz} {Level:u3}] {Message:lj} {Properties:j}{NewLine}{Exception}"
        }
      },
      {
        "Name": "File",
        "Args": {
          "path": "logs/wms-.log",
          "rollingInterval": "Day",
          "retainedFileCountLimit": 30,
          "fileSizeLimitBytes": 104857600,
          "rollOnFileSizeLimit": true,
          "outputTemplate": "[{Timestamp:yyyy-MM-dd HH:mm:ss.fff zzz} {Level:u3}] [{SourceContext}] {Message:lj} {Properties:j}{NewLine}{Exception}",
          "shared": true
        }
      }
    ],
    "Enrich": [ "FromLogContext", "WithMachineName", "WithThreadId" ]
  },
  "SecurityHeaders": {
    "ContentSecurityPolicy": "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com https://cdn.jsdelivr.net data:; img-src 'self' data: blob:; media-src 'self' blob:; connect-src 'self'; frame-ancestors 'none'",
    "FrameOptions": "DENY",
    "ContentTypeOptions": "nosniff",
    "ReferrerPolicy": "strict-origin-when-cross-origin",
    "PermissionsPolicy": "camera=(self), microphone=(), geolocation=(), payment=()"
  },
  "AllowedHosts": "*"
}
