# Tech Debt Tracker

Track technical debt items - things we've intentionally deferred or
shortcuts taken that need cleanup later. Updated as discovered,
closed when fixed.

## Conventions

- **ID format**: TD-NNN sequential (TD-001, TD-002, etc.)
- **Reference in code**: Use comment `// TODO(TD-XXX): description` near affected code
- **Add immediately**: Don't wait — log when discovered
- **Close with commit**: Move to Closed section with commit hash
- **Review**: Quarterly or before each release

## Status Legend

- 🔄 **Open** - Active debt, scheduled for fix
- ⚠️ **Watching** - Acceptable for now, monitor for triggers
- ✅ **Closed** - Resolved
- ❌ **Wontfix** - Acknowledged but won't be fixed (with reasoning)

---

## Open Items

| ID | Title | Priority | Discovered | Plan | Notes |
|----|-------|----------|------------|------|-------|
| TD-004 | Putaway StockMovements rows carry ReferenceId = NULL | Medium | 2026-05-08 | Closes when ADR-004 (hybrid putaway template + scoring) lands and introduces a putaway header table; backfill via UPDATE matching ReferenceType='Putaway' AND PerformedAt range is then trivial | Per ADR-014. Pinned by PutawayServiceTests.PutawayStockAsync_PassesNullReferenceId_TD004 |
| TD-005 | ADR-004 (Hybrid Putaway: Template + Scoring) missing as a doc | Low | 2026-05-08 | Draft alongside the suggestion-engine implementation. Referenced by name in `docs/01_WMS_Master_Design.md` and `IPutawayService` comments but no file in `docs/decisions/` | Discovered during ADR-014 audit |
| TD-006 | StockMovements write-path integration tests need a real SQL Server fixture | Low | 2026-05-08 | Stand up testcontainers / LocalDB fixture when ADR-013 / ADR-012 implementations land — they'll need it too. Then remove the `Skip` attributes on `StockMovementLogTests` (5 cases already authored, intent-complete). **Phase 7 expansion**: same fixture also unlocks SqlException 2627 / 2601 / 547 catch-path tests in `ProductsAdminTests` / `CustomersAdminTests` / `WarehousesAdminTests` — currently uncovered because Microsoft.Data.SqlClient's SqlException can't be reflection-constructed cleanly across client versions | Currently relying on manual smoke + Moq-level context-binding tests in unit suite |
| TD-007 | `/Receive` mobile PWA form lacks `wms-custom.css` styling | Low | 2026-05-08 | Apply mobile-first design-system styling to the receive form. Define the small-screen variants needed (compact form fields, scanner-friendly tap targets, inline validation messages) and replace the raw Bootstrap `card`/`form-control` classes. Out of scope: anything desktop-shell shaped — this IS the handheld scanner workflow per CLAUDE.md "Mobile PWA, not Native" architecture | **Rewrite 2026-05-09**: original entry prescribed migrating to `_OfficeLayout`, which would break the page's purpose. `_ViewStart` correctly pins `_MobileLayout` + the Receiver PWA manifest at `wwwroot/receive/manifest.json`. The visual inconsistency the original entry flagged is real — forms use raw Bootstrap — but the fix is mobile styling, not a desktop layout migration. Logged inside Phase 6E hygiene |
| TD-008 | `/Putaway` mobile PWA form lacks `wms-custom.css` styling | Low | 2026-05-08 | Same scope as TD-007 — apply mobile design-system styling to the putaway form. Receiver and Picker PWAs share visual language, so TD-007 + TD-008 likely close in one pass | **Rewrite 2026-05-09**: same misframing as TD-007. `_ViewStart` correctly pins `_MobileLayout` + the Picker PWA manifest at `wwwroot/putaway/manifest.json`. Logged inside Phase 6E hygiene |
| TD-009 | `master.Warehouses` has no `Status` string — UI lost the "maintenance" intermediate state | Low | 2026-05-08 | If business needs "maintenance" back: add `Status` NVARCHAR(20) NOT NULL DEFAULT 'Active' + CHECK ('Active'/'Maintenance'/'Inactive'); migrate `IsActive=1`→`Status='Active'`, `IsActive=0`→`Status='Inactive'`; drop `IsActive`. Frontend filter chip + statusBadge already accept 'maintenance' as a no-op so the migration is purely additive | Phase 6B intentionally collapsed mock's 3 states (`active`/`maintenance`/`inactive`) onto the existing `IsActive` bool. WarehouseStatusMapper.FromWire("maintenance") returns null (drops the filter); pinned by `WarehousesControllerTests.GetData_UnknownStatus_DropsFilter_TD009` |
| TD-011 | Customer.TotalOrders / LastOrderDate / Last-order-relative stubbed `null` / `"—"` | Low | 2026-05-08 | Wire when an orders schema lands (Phase 7+). Same pattern as TD-010 — repo first, controller swap is small. Detail stat tiles (Total orders, YTD revenue, Last order, Avg ticket) all stubbed `"—"` and pinned by `CustomersControllerTests.Detail_StatTiles_AllStubbed_TD011` | No outbound order schema exists today; mock had fake values like `TotalOrders × 4500` for "YTD revenue" — preserved as stub until real data flows |
| TD-012 | Product price column missing — pricing is owner-scoped on `master.ProductOwners.SettlementPrice` | Low | 2026-05-09 | When admin CRUD lands (Phase 7+), resolve price via the customer/owner context active on the request. List view `fmtPrice(null)` already renders `"—"` so the column degrades cleanly until then. Detail page Price tile dropped entirely | Discovered during T8. Mock used a fake random per-product price; schema design has no such column because pricing legitimately differs per Owner-Product mapping (3PL/VMI vs Self) |
| TD-014 | Activity tab on Customer Detail still on hardcoded mock entries (Warehouse half closed Phase 6E) | Low | 2026-05-09 | Remaining Customer half needs an outbound-orders + CRM-notes stream — blocked on Phase 7+ orders schema. Once orders + invoices land, the Warehouse-side composition pattern (controller composes typed repo queries → mappers → merged sort) ports cleanly: add `IOrderRepository.GetActivityByCustomerAsync` + `OrderActivityMapper`, plug into `CustomersController.Detail` alongside any future CRM-notes source | **Warehouse half closed 2026-05-09** by Phase 6E (commits c665e0b + ef3a325 + dc5f30a): merged feed across `inbound.ReceivingHeaders` + `inventory.StockMovements`, composed in C# (Q1 strategy). Customer half still on hardcoded entries pinned by `Detail_NoMovements_ActivitiesEmpty_TD010Closed`-style regression that doesn't yet exist for Customer — would land with the wiring |
| TD-016 | Putaway operations render as 2 separate rows on the Activity tab — no pairing into a single logical entry | Low | 2026-05-09 | Naturally closes alongside TD-004: once ADR-004 introduces a putaway header table, the source + destination movements share a `ReferenceId`, and the renderer can group by `(ReferenceType, ReferenceId)` to fold paired rows into a single "moved {qty} from {from} to {to}" entry. Until then, splitting the location clause by `QuantityDelta` sign keeps each row grammatical (source→"from STAGE-01", dest→"into BIN-A1") | Phase 6D mapper choice. Both rows DO have FromLocationId + ToLocationId populated (per Phase 6A's putaway writer), so a future grouping pass has the data it needs without a SQL change |
| TD-017 | Quick Action buttons on Detail page sidebar — 7 of 12 still inert (target routes don't exist) | Low | 2026-05-09 | Remaining 7 buttons wait on Phase 7+ routes: Products `Adjust stock` + `Print label`; Customers `Create order` + `View invoices`; Warehouses `Receive shipment` + `Cycle count` + `View stock`. Each ~30 min once its target endpoint exists. Adjust stock could close once ADR-013 General Stock Adjustment lands; Cycle count alongside the counts-module phase | Discovered Phase 6D smoke. **Phase 6E partial close**: wired Receive stock + Send email (2 of 9). **Phase 7 partial close 2026-05-09**: 3 new "Edit {entity}" actions added to Products / Customers / Warehouses Detail pages, all wired to `/{Entity}/Edit/{code}`. Total surface now 12 actions; 5 wired (Receive, Edit×3, Send email), 7 remaining inert. The Detail layout disabled-state styling (muted opacity, cursor:not-allowed, "Coming in a future phase" tooltip) keeps the inert ones visible without misleading the operator |
| TD-018 | Warehouse picker empty-state visibility uses brittle DOM-attribute query | Low | 2026-05-09 | Refactor the picker view to track filter state in Alpine reactively: build a `haystacks` JS array at render time, expose a `visibleCount` computed via `.filter(...)`, drive empty-state x-show off `visibleCount === 0 && search.trim()`. Alternative: switch the rows themselves to Alpine x-for. Either approach is ~30 min; current DOM-query works in tested browsers but depends on Alpine setting inline `style="display: none"` exactly | Phase 8.5 picker. The current x-show on `.wmsp-empty` reads `document.querySelectorAll('.wmsp-row[style*="display: none"]').length === @totalCount` — relies on Alpine's exact inline-style mutation pattern. If Alpine ever ships a normalised whitespace variant or uses a class instead, the empty state stops appearing. Visible-row count tracked reactively in Alpine state would close this cleanly |
| TD-019 | Recent warehouses section on picker deferred — no persistence layer | Low | 2026-05-09 | Add a small `picker_recent_warehouses` cookie (or `IUserPreferences` table) that records the last 3 warehouses the user picked, scoped to UserId × TenantId. Render at the top of the picker if non-empty. Phase 8.5 brief asked for this but the storage layer doesn't exist yet | Phase 8.5 brief D. Skipped pragmatically — no warehouse-pick history persisted anywhere. Cookie-based approach is the lightest implementation path; user-prefs table is the cleaner long-term shape |
| TD-024 | ASN (Advanced Shipping Notice) layer missing | Low | 2026-05-09 | Mid-market WMS often skips ASN entirely (PO → GR direct flow, which is what we have). When a tenant integrates with an EDI-shipping vendor or wants pre-arrival visibility: add `inbound.InboundDeliveries` table + service + list/detail/create UI. ASN-to-PO + ASN-to-GR linking. Phase 11+ if business demands | Phase 9 audit flagged. Owner-aware ASN with multi-line + container scope |
| TD-025 | Vendor master is informal — uses `master.Owners` with OwnerType='Supplier' | Low | 2026-05-09 | When business needs vendor-specific data (lead time, MOQ, defect rate, primary contact, payment terms): either (a) add a `master.Vendors` table with FK to Owners + vendor-extension columns, or (b) extend Owners with conditional vendor-only columns (lead-time / MOQ / etc., NULL for non-supplier types). Phase 12+ if business demands | Phase 9 audit flagged. Phase 9A picker uses `IOwnerRepository.GetActiveSuppliersAsync` filtered to OwnerType IN ('Supplier','VMI') + IsActive=1 — works fine for the dropdown surface but offers no vendor-management UI |
| TD-026 | PO Edit "lines locked" is full-PO lock, not per-line | Low | 2026-05-09 | Phase 9A brief Q2 chose full-PO lock when ANY line has ReceivedQuantity > 0 — simplest safe path. Per-line lock is kinder UX (locked lines stay locked; new lines + zero-received lines remain editable), but requires receipt-aware validation (a line being edited can't have its UoM change in a way that invalidates existing receipts; ExpectedQty can't drop below ReceivedQty; etc.). Phase 10 target with the GR cancellation flow | Phase 9A T8-T13. Service throws InvalidOperationException on ReplaceLines when CountReceivedLinesAsync > 0; Edit form's UI banner explains "cancel-and-recreate is the path forward" |
| TD-027 | GR Edit-Draft-Promote flow not implemented | Low | 2026-05-09 | Add a `PostExistingDraftAsync(headerId)` service method that reads an existing Draft ReceivingHeader, runs the per-line stock orchestration (Phase 9B Steps 2-4 currently inside PostReceivingAsync), updates header Status='Draft' → 'Posted', and triggers PO status transitions. Plus an Edit endpoint on /Receiving/Detail that re-opens the GoodsReceipt/Create form with the Draft pre-populated. Phase 10 target | Phase 9B's IsDraft path creates Draft headers but offers no path to promote them. Drafts can be viewed via /Receiving/Detail but not edited. The Quick Action "Edit draft" stays disabled until this lands |
| TD-031 | Adjustment workflow — sub-features deferred from Phase 11A v1 | Low | 2026-05-10 | Five sub-features intentionally left out of v1 to keep scope at ~1-2 days: (a) Threshold-based auto-approval for small deltas (e.g. ≤$50 worth → self-approve); (b) Role-based approval bypass (e.g. WarehouseManager can self-approve); (c) Multi-line adjustments (current is single-line; cycle-count batches use the future counts.CountAdjustments table per ADR); (d) Stock-detail-page entry point (Stock detail UI doesn't exist yet — Adjustment Create remains standalone-form-only); (e) Billing hooks per ADR-013 — Phase 12+ when billing module lands. Each is independent and can land separately as discrete chunks | Phase 11A v1. ADR-013 spec mentions all five but only the core "create / approve / reject with reason" + "TransactionScope on Apply" + "Movement Log integration" + "separation of duties" landed. Functional path complete; these are extensions |
| TD-032 | Cycle Count — sub-features deferred from Phase 12 v1 | Low | 2026-05-10 | Six sub-features intentionally left out of v1 to keep scope at ~1.5-2 days: (a) Mobile PWA scan-driven counting (current is desktop inline form); (b) Multi-counter coordination (single counter per session in v1; per-line CountedBy on lines table would enable shared sessions); (c) Per-line approve/reject (whole-session apply only); (d) Print count sheet (clipboard-friendly PDF/HTML — like Phase 9C GRN print); (e) Scheduled / recurring counts (cron-driven session creation; ABC class scoping); (f) Variance threshold gating (e.g. >10% variance requires extra approver). Each independent | Phase 12 v1. Core workflow (snapshot → count → submit → apply with separation of duties) shipped. The deferred items are productivity / scale features, not correctness gaps |
| TD-045 | Stock VALUE roll-up missing on Inventory report — Product entity has no Cost field | Low | 2026-05-11 | Pricing lives in `master.ProductOwners.SettlementPrice` (owner-scoped per ADR-007 — a single product can have different settlement prices per Owner). To compute "stock value": JOIN `inventory.Stock → master.ProductOwners` on (ProductId, OwnerId) → SUM(QuantityOnHand × SettlementPrice). Caveats: NULL ProductOwners row (product not yet onboarded for the owner) needs a fallback; CostBasis = SettlementPrice is a domain decision (could differ from Cost). Add when enterprise tenants ask for value vs quantity. Phase 23 v1 reports stock quantity instead — simpler, no cost-basis decision needed | Phase 23 v1 Inventory dashboard. Brief mentioned "total stock value (current_qty × product_cost)"; reality is Product has no Cost. 6th-instance spec rename audit triggered (per `feedback_spec_rename_audit.md`) — silently applied: report shows quantity, logged here. Owner-scoped value rollup is a real feature, not a workaround |
| TD-046 | Reports permission is single REPORTS.VIEW — no per-report split | Low | 2026-05-11 | Three sub-functions could replace the single VIEW: REPORTS.INVENTORY, REPORTS.ORDERS, REPORTS.KPIS. Each gets its own RoleFunctionPermissions row + the controller decorator. Enterprise tenants may want to grant "finance sees Order Analytics, ops sees KPIs, neither sees Inventory" — typical separation of duties pattern | Phase 23 v1. Single permission keeps Phase 23 simple (one migration, one decorator). Splitting is mechanical when the requirement surfaces |
| TD-047 | Reports — sub-features deferred from Phase 23 v1 | Low | 2026-05-11 | Eight sub-features intentionally left out of v1 to keep scope at ~6-8 hours: (a) Custom date range picker (Flatpickr — current is fixed presets only); (b) PDF export (rendered via FastReport or wkhtmltopdf — current is Excel only); (c) CSV export per report; (d) Scheduled reports (Hangfire-driven — emit Excel to email or shared drive on cron); (e) Saved filter sets per user; (f) Per-warehouse + per-customer filter on Inventory + Orders + KPIs; (g) Drill-through links from charts to source records (e.g. clicking a top customer → /Customers/Detail/{code}); (h) Real-time auto-refresh / SignalR push for KPIs board view. Each independent | Phase 23 v1. Core reports shipped with charts + Excel export + fixed-preset filters. The deferred items are enhancement/scale features, not correctness gaps |
| TD-048 | Reports — aggregation queries lack integration tests | Low | 2026-05-11 | 13 SQL aggregation methods on `IReportRepository` are exercised by Moq-based controller tests but the SQL itself isn't covered. Same TD-006 family — needs a real SQL Server fixture. Risk is low (read-only, idempotent, no mutations) but a regression in `GetCycleCountVarianceAsync`'s 4-subquery shape (for example) wouldn't be caught at build time. Phase 23 ships with manual browser smoke only | Phase 23 v1. Acceptable trade-off — controller plumbing covered, SQL relies on browser smoke + future SQL fixture |
| TD-049 | Password change UI missing on User Edit | Medium | 2026-05-12 | Phase 25 security hardening. Two paths: (a) admin force-set ("reset password" Quick Action on Detail → modal with new-password textbox → `_auth.HashPassword` + `User.PasswordHash` update + AuditLog event 'UserPasswordReset' + force `FailedLoginAttempts=0`), (b) self-service ("Change password" on a future /Account page → verify current + set new). Both gated by SECURITY.USERS.Edit for admin path, no perm needed for self | Phase 24 v1. Today's Edit form omits Password field with a hint "Use Unlock from Detail to clear lockout. Password change is a Phase 25 feature." |
| TD-050 | Custom role creation missing (only system-role permission editing today) | Low | 2026-05-12 | Add POST /Roles (admin-only) with Code + Name + Description + initial permission grants. Role Edit (Code is immutable post-create; Name + Description + IsActive are editable). Delete: only allowed when zero UserRoles reference the role + role isn't system. ~3-4h. Most tenants are fine with PICKER/PACKER/MANAGER/ADMIN baseline; need surfaces when an enterprise asks for "Receiving Supervisor" or similar tier | Phase 24 v1 — system roles only. Backend permission upsert + audit already in place; just needs CRUD UI |
| TD-051 | Time-bounded role assignments UI missing | Low | 2026-05-12 | `UserRoles.ValidFrom` + `ValidTo` columns exist (migration 038); Phase 24 assigns both as NULL = effective immediately, no expiry. Future UI: date pickers per-role on User Create/Edit, "Expires in N days" display chips on Detail, permission resolver already filters `WHERE NOW BETWEEN ValidFrom AND ValidTo` (verify) | Phase 24 v1. Date-bounded role useful for contractors / seasonal staff |
| TD-052 | Bulk user operations missing | Low | 2026-05-12 | CSV upload for user creation (email + name + initial role + initial password); mass-deactivate by role; mass role re-assignment. Phase 24 v1 is per-row only | Phase 24 v1 |
| TD-053 | AuditLog Details JSON viewer is flat monospace | Low | 2026-05-12 | Today Details is rendered as `System.Text.Json.JsonSerializer.Serialize(parsed, WriteIndented=true)` into a `<pre>` block. Future: collapsible tree (vanilla JS or a tiny lib like json-formatter-js — ~5KB), syntax highlighting per key/value type, copy-to-clipboard button. Operator-friendly when Details is deeply nested (rare today — Phase 24 emits flat 2-4 key payloads) | Phase 24 T3. Pretty-print is "good enough" for the flat shapes SecurityService writes |
| TD-054 | Per-user Activity feed (User Detail) deferred | Low | 2026-05-12 | New tab on Users/Detail/{id} listing the user's audit events (where UserId=actor OR EntityId=user). Phase 24 ships Roles tab + Quick Actions sidebar on Detail; Activity tab would be a 3rd tab using the existing `_DetailLayout` framework. Sourced from `IAuditLogRepository.GetPagedAsync(filter with UserId=...)` — repo already supports it | Phase 24 v1. Operators can today filter /AuditLog by user via the dropdown (planned URL-param wiring is TD); a direct cross-link from User Detail would be nicer |
| TD-055 | 2FA / TOTP not implemented | Medium | 2026-05-13 | v3.1+ feature. Schema: `security.UserTotpSecrets` (UserId PK, EncryptedSecret, EnrolledAt, Recovery codes). Enrolment: QR code render via `QRCoder` lib, secret generated server-side, confirmation step with first OTP. Login: after password verify, redirect to /Auth/Verify2FA if user has TOTP enrolled. Recovery codes one-shot use. ~1 week incl. recovery flow + admin disable | Phase 25 D3 — explicitly skipped. Enterprise compliance gate at v3.1; pre-launch enterprise prospects accept "no 2FA today" given a stated v3.1 commit |
| TD-056 | Email password reset flow missing | Medium | 2026-05-13 | Needs SMTP infrastructure (likely SendGrid / Postmark integration via a small `IEmailService` interface). Flow: /Auth/ForgotPassword → email lookup → if exists, generate single-use token (`master.PasswordResetTokens` table, 1h TTL) + email link → /Auth/ResetPassword?token=... → password policy + commit. Phase 26 deployment groundwork (SMTP secret mgmt) is a prereq | Phase 25 v1. "Forgot password?" link in Login view currently href="#"; explicit defer |
| TD-057 | Password policy per-tenant config | Low | 2026-05-13 | New `security.SystemSettings` rows: `password.min_length`, `password.require_uppercase`, `password.require_digit`, etc. `PasswordPolicy` becomes per-call (loads settings via `ITenantContext`). Allows enterprise tenant to enforce stricter policy than baseline | Phase 25 v1. Baseline 8/mixed/digit is the floor; tenants who want stricter can layer on top |
| TD-058 | Password history (no-reuse last N) | Low | 2026-05-13 | Schema: `security.PasswordHistory(UserId, HashedPassword, ChangedAt)`. On ChangePassword + ResetPassword: compare new pw against last N hashes (config; default 5); reject if any match. Migration adds history-write side-effect to `UpdatePasswordHashAsync` | Phase 25 v1. Enterprise expectation but rare for SMB |
| TD-059 | Password expiration / forced rotation | Low | 2026-05-13 | New column `Users.PasswordChangedAt` (defaults to CreatedAt for legacy rows). Periodic Hangfire daily job flags users whose pw is older than `password.expiry_days`. Login middleware redirects flagged users to `/Account/ChangePassword` before completing auth flow | Phase 25 v1. Compliance feature; orthogonal to TD-057 |
| TD-060 | LoginRateLimiter is in-process IMemoryCache | Medium | 2026-05-13 | Singleton MemoryCache works for single-instance deployments. Multi-instance (load-balanced) needs a shared store: Redis or `IDistributedCache`. Same `ILoginRateLimiter` interface, swap implementation. ~2-3h once the deployment target is known | Phase 25 v1. Acceptable for v3.0.0 single-server enterprise launch. Phase 26 deployment will surface the requirement |
| TD-061 | Per-tenant session length config | Low | 2026-05-13 | Cookie ExpireTimeSpan is global today (8h). Per-tenant override via `security.SystemSettings.session_hours`. Implementation: custom `ICookieAuthenticationEvents` reads tenant settings on `SigningIn` and adjusts `AuthenticationProperties.ExpiresUtc` | Phase 25 v1. Most tenants happy with 8h default |
| TD-062 | Forbidden-attempt audit logging | Low | 2026-05-13 | `[Authorize]` failures + `RequirePermissionAttribute` rejections currently produce a redirect with no AuditLog row. Future: emit `AccessDenied` events with FunctionCode + attempted Action + IP. Visible spike in this event-type signals reconnaissance or misconfigured perms | Phase 25 v1. Operationally useful; not strictly required for v3.0.0 launch |
| TD-063 | Logout + session-timeout audit logging | Low | 2026-05-13 | `AuthController.Logout` currently calls `HttpContext.SignOutAsync` with no AuditLog emit. Add `LogoutSelf` event. Session timeout is browser-driven so no server-side event — would need a heartbeat ping. Lower priority than TD-062 | Phase 25 v1 |
| TD-064 | SuperAdmin tenant management deferred to Phase 27 | Low | 2026-05-13 | Cross-tenant operations (create new tenant, list all tenants, suspend tenant, force-reset SUPERADMIN access) live in master DB; need a separate SuperAdmin login flow + dedicated controllers under /SuperAdmin/. Phase 27 onboarding territory | Phase 25 v1. **CLOSED by Phase 27** — see v2.13.0-onboarding |
| TD-065 | Hybrid DB strategy deferred to v3.1+ | Low | 2026-05-14 | DB-per-tenant (ADR-001) chosen for v3.0.0 enterprise launch. Hybrid (shared DB with TenantId column for SMB) explored only when SMB expansion arrives | Phase 26 D3. Architectural change; not a code-debt cleanup |
| TD-066 | Azure Key Vault / Windows DPAPI for secrets | Medium | 2026-05-14 | Env vars in web.config are visible to anyone with file-system access. Better: Key Vault (cloud) / DPAPI (Windows-local). `Azure.Extensions.AspNetCore.Configuration.Secrets` for KV; ~3-4h to wire | Phase 26 v1. Acceptable for v3.0.0 single-server launch with restricted file-system access |
| TD-067 | CI/CD pipeline (GitHub Actions) | Medium | 2026-05-14 | Manual deploy works for 1-2 customers. Pipeline: on tag push → build + test + publish → deploy to staging → manual approval → production. Health-endpoint smoke after each deploy | Phase 26 D4. Adds when scale dictates |
| TD-068 | Application Insights monitoring | Medium | 2026-05-14 | Serilog file logs cover the 'what happened'. App Insights adds distributed tracing, dependency health, custom dashboards, alerting. Pair with TD-067 | Phase 26 D5. File logs sufficient for MVP enterprise launch |
| TD-069 | IIS deployment automation scripts | Low | 2026-05-14 | PowerShell DSC / Octopus / xcopy + `Stop-WebAppPool`/`Start-WebAppPool` cycle. Phase 26 docs cover manual steps; automating for second customer | Phase 26 v1 |
| TD-070 | Production seed data tooling | Low | 2026-05-14 | A 'minimal viable tenant' SQL script library. Phase 27 onboarding produces this implicitly via TenantProvisioningService — could extract to a standalone migrator helper | Phase 26 v1. **Partially CLOSED by Phase 27** — bootstrap admin seeding works; warehouse/category/UoM defaults are still per-migration |
| TD-071 | Backup automation | Low | 2026-05-14 | Manual per checklist.md. SQL Server Agent jobs / Ola Hallengren maintenance scripts; document storage backups via robocopy / Azure Blob lifecycle | Phase 26 v1 |
| TD-072 | Distributed cache (Redis) for multi-instance | Medium | 2026-05-14 | IMemoryCache breaks under load-balanced multi-instance: tenant connection cache + LoginRateLimiter + future caches diverge across nodes. Redis or SQL Server distributed cache. Same ILoginRateLimiter interface — swap impl | Phase 26 v1. Acceptable for single-server launch |
| TD-073 | Performance benchmarking baseline | Low | 2026-05-14 | Before customer #1: run NBomber / k6 against populated staging DB to establish p50/p95/p99 baselines for headline pages. Tracks regression post-launch | Phase 26 v1 |
| TD-074 | Auto-scaling configuration | Low | 2026-05-14 | Azure App Service / IIS web farm / Kubernetes — different shapes per host. Single-instance fine for v3.0.0; scale-out when MAU > 100 concurrent | Phase 26 v1. Hosting decision drives the shape |
| TD-075 | Disaster recovery runbook | Medium | 2026-05-14 | Documented procedures for: SQL Server failure, IIS host failure, corrupted tenant DB, hostile data event. DR drill per customer per quarter | Phase 26 v1. Enterprise customers will ask in procurement |
| TD-076 | Edit tool occasionally fails with 'File has not been read yet' | Low | 2026-05-14 | Recurring tool-level flake: after a successful Edit, immediate follow-up Edit occasionally fails with 'File has not been read yet'. Workaround: Read again (5-line slice) before next Edit. Not codebase | Process note |
| TD-077 | Async tenant provisioning via Hangfire | Low | 2026-05-15 | Phase 27 ships synchronous (~10-30s). At scale > 100 tenants, move to Hangfire fire-and-forget. Operator submits → instant 'queued' → poll status. Requires `master.TenantProvisioningJobs` tracking table | Phase 27 v1. UI loading state covers the wait for low-scale launch |
| TD-079 | Hard delete tenant (DROP DATABASE) | Medium | 2026-05-15 | Today Suspend preserves data; reactivate restores access. Hard delete drops the DB + deletes master.Tenants + clears UserTenantMap. Two-step UX (confirm with code retype + 24h grace). Audit before drop. Phase 30+ | Phase 27 v1 |
| TD-080 | Archived tenant state (read-only) | Low | 2026-05-15 | New Status enum value: `Archived`. Tenant Users login but every CRUD action rejects. Legal-hold scenarios | Phase 27 v1 |
| TD-081 | Resource limits per tenant | Low | 2026-05-15 | `master.TenantQuotas` table (MaxUsers / StorageQuotaMB / RequestRateLimit). Middleware checks quota on each request. Trial tenants get low quotas; paid tenants higher | Phase 27 v1 |
| TD-082 | Per-tenant statistics dashboard | Low | 2026-05-15 | New SuperAdmin page: pick a tenant → see user count / doc storage size / order volume last 30d / last activity. Cross-DB reads. Read-only for sales/account-management | Phase 27 v1 |
| TD-083 | Tenant settings UI (timezone, currency, fiscal year) | Low | 2026-05-15 | SuperAdmin form: per-tenant settings beyond basics. Tenant DB SystemSettings table from Phase 1 — just needs UI. Common asks for enterprise customers in different regions | Phase 27 v1 |
| TD-084 | Tenant cloning | Low | 2026-05-15 | Useful for trial → paid migration. Copy schema + master data + zero operational data. 'Start from another tenant' option on Create flow | Phase 27 v1 |
| TD-085 | Tenant data export | Low | 2026-05-15 | Compliance + customer-leaving. Zip of CSVs (Users / Stock / SalesOrders / etc.) + document files. Async via Hangfire. Temporary signed URL | Phase 27 v1 |
| TD-086 | Soft delete with N-day recovery window | Low | 2026-05-15 | 'Schedule deletion' marks tenant with DeletionScheduledAt. Daily Hangfire job drops databases past grace period. 7-day SuperAdmin notification. Pair with TD-079 | Phase 27 v1 |
| TD-087 | SuperAdmin Activity tab (per-SuperAdmin audit feed) | Low | 2026-05-15 | Sibling of TD-054. New page showing one SuperAdmin's history. Sourced from master.SystemAuditLog filtered by UserId. Change-control reviews | Phase 27 v1 |
| TD-088 | SuperAdmin user management UI | Low | 2026-05-15 | Today: config-driven first-run seed only. Subsequent additions need direct DB insert. Future: `/SuperAdmin/Admins` CRUD page. Lock-out protection: refuse deactivation of last active SuperAdmin | Phase 27 v1. 1-2 SuperAdmins handled out-of-band for v3.0.0 |
| TD-089 | Thai-language customer-facing docs | Low | 2026-05-16 | When first Thai customer signs. Translates onboarding-playbook + user guides + UI strings. Today: English-primary per ADR-020 | Phase 28 v1 |
| TD-090 | User guide per module (post-first-customer) | Medium | 2026-05-16 | Per-module guides (Inbound / Inventory / Outbound / Reports / Mobile). Structured around the first customer's actual questions. Markdown + screenshots. Goal: replace 1:1 calls with self-service reading | Phase 28 v1. ADR-020 — customer-driven content > generic upfront content |
| TD-091 | Video tutorials | Low | 2026-05-16 | Post-launch when UI is stable. ~5-min walkthrough per major workflow. Hosted on YouTube / Loom or self-hosted. Best produced WITH the first customer (real screen, real data) | Phase 28 v1 |
| TD-092 | API documentation | Low | 2026-05-16 | When/if we expose a public API for integrations. OpenAPI / Swagger spec + Postman collection. Needs API surface first; today everything is server-rendered MVC | Phase 28 v1. Pair with the eventual API exposure phase |
| TD-093 | Knowledge base / FAQ | Low | 2026-05-16 | Public FAQ + searchable knowledge base. Populated from real customer support questions. Tools: GitBook / ReadMe / Notion public pages / simple Markdown in docs/ | Phase 28 v1 |
| TD-094 | Translated onboarding materials | Low | 2026-05-16 | Onboarding playbook + sales decks in customer's language. Triggered by first international customer signing | Phase 28 v1 |
| TD-095 | Marketing site content | Low | 2026-05-16 | v3.1+ SaaS launch needs public-facing site (landing pages, feature pages, pricing, case studies). Today: no public marketing presence | Phase 28 v1 |
| TD-096 | Architecture diagrams (visual) | Low | 2026-05-16 | Replace the README ASCII diagram with proper visual diagrams (Lucidchart / draw.io / Mermaid). Valuable for sales conversations + enterprise procurement decks | Phase 28 v1. Mermaid is good-enough first step (Markdown-native, GitHub-rendered) |
| TD-097 | Master Data Index pages have dead 'Add new' cards | Low | 2026-05-17 | Customers/Products/Warehouses Index.cshtml have a `wms-add-card` link with `href="#"` that overlaps with the page-header "New X" button. Either remove the duplicate or make the inline card trigger the same /Create route | Phase 29 v1. Cosmetic; doesn't block demo |
| TD-098 | Login page 'Forgot password?' + 'Contact support' links dead | Low | 2026-05-17 | Both have `href="#"` on the login surface. Forgot password = TD-056 (needs SMTP). Contact support = TD-093 (knowledge base + support contact landing page). Both will land naturally with their parent TDs | Phase 29 v1 |
| TD-099 | Decision-modal Cancel buttons use `href="#"` | Low | 2026-05-17 | CSS-only `:target` modal pattern uses anchor close behavior — intentional but visually inconsistent (looks like a link, behaves like a button). Same in 7 partials: Adjustment / CycleCount / Pack / Pick / Receipt / SalesOrder / Transfer decision modals. Wrap as `<button type="button">` styled like the existing `wms-*-btn` classes | Phase 29 v1. Pattern works; cosmetic gap |
| TD-100 | SUM(CASE WHEN) NULL-safety sweep across all chip-count repos | Low | 2026-05-17 | 9 repos use the Phase 23-24 chip-count pattern with bare SUM(CASE WHEN ... THEN 1 ELSE 0 END). Returns NULL on empty result → Dapper InvalidCastException. SuperAdminController fixed in Phase 29 (TD-100 covers the rest: PurchaseOrder / Receiving / SalesOrder / PickTask / PackTask / Shipment / Adjustment / CycleCount / TransferOrder). Low-risk because seed migrations always provide at least 1 row, but a defensive sweep wraps all in ISNULL(..., 0) | Phase 29 v1 |
| TD-101 | Secure delivery channel for temp passwords | Medium | 2026-05-17 | Onboarding playbook references Signal / Slack DM / 1Password for sharing temp passwords. v3.1 — ship a one-time-view share-URL with TTL expiry. Server generates random ID, stores temp password encrypted, recipient gets a single-use URL; second visit returns "already viewed" | Phase 29 v1. Out-of-band works for sales-led launch; needed when SaaS funnel widens |
| TD-102 | `_OfficeLayout` dropdown anchor accessibility | Low | 2026-05-17 | The `wms-topbar-avatar` Bootstrap dropdown trigger uses `<a href="#">` for `data-bs-toggle="dropdown"`. Works fine but semantically should be `<button>` (no navigation; the # is purely Bootstrap's marker). Accessibility audit would flag it. Bootstrap accepts buttons; no behavior change | Phase 29 v1 |
| TD-103 | MailKit migration (close SYSLIB0014 deprecation warning) | Low | 2026-05-18 | `GmailSmtpEmailService` uses `System.Net.Mail.SmtpClient` (marked obsolete in .NET 8 — SYSLIB0014). Accepted via `#pragma warning disable` for Phase 30A; full MailKit migration is ~30 LOC mechanical. Pair with TD-110 SendGrid switch or do as separate refactor | Phase 30A v1. Suppressed locally; works fine |
| TD-104 | Email retry queue (Hangfire-driven) | Medium | 2026-05-18 | Today: best-effort send; SMTP failure → swallow + log Warning, no retry. For production scale: enqueue email send as Hangfire job; built-in retry policy (3 attempts with exponential backoff) + dead-letter queue for inspect/replay. Pair with TD-110 — at SendGrid scale, transient failures become routine | Phase 30A v1. Sync best-effort fine for first customer |
| TD-105 | Email send audit log table | Medium | 2026-05-18 | New tenant table `security.EmailAuditLog` (To, Subject, CorrelationId, SentAt, Status, FailureReason). Today: only Serilog records; no per-tenant queryable view. Auditability for "did the customer ever receive that password?". Pair with TD-104's job tracking | Phase 30A v1. Logs sufficient for v3.0.0 forensic recovery |
| TD-106 | DKIM / SPF / DMARC setup docs | Low | 2026-05-18 | Production sender domain needs DKIM-signed outgoing mail + SPF allowlist + DMARC policy to avoid spam-folder routing. Today: relies on Gmail's reputation. For owned-domain FromAddress, add per-domain DNS setup procedure to `docs/deployment/`. Critical when customer's IT flags WMS mails as suspicious | Phase 30A v1. Gmail-relayed mails arrive in inbox without it |
| TD-107 | Per-tenant email template customization UI | Low | 2026-05-18 | Today: 4 templates baked into WMS.BLL embedded resources. Tenants may want branded headers/footers, custom signature, alternative language. Add `documents.EmailTemplates` per-tenant table + Razor-based template renderer (replace `{{Placeholder}}` substitution). Customer ask: "make the welcome email say 'ACME Corp' not 'WMS'" | Phase 30A v1. Default templates fine for sales-led launch |
| TD-108 | User email preferences / unsubscribe | Low | 2026-05-18 | GDPR-flavoured: end users (operators) should be able to opt out of non-transactional notifications. Today: all emails are transactional (provisioning, password reset) — unsubscribe doesn't apply. When we add operational notifications (stock alerts, daily summaries, etc.), each needs a preference flag + unsubscribe footer | Phase 30A v1. v3.1+ when notification volume grows |
| TD-109 | Welcome email on tenant user CREATE | Low | 2026-05-18 | The `Welcome.html`/`txt` templates ship in this phase but aren't wired — only TenantCreated + PasswordReset are emitted. Wire `Welcome` into `SecurityService.CreateUserAsync` so newly-added team members get an intro + their temp password by email. Today: ADMIN sets a password manually + tells the user out-of-band | Phase 30A v1. Templates ready; integration point one method call |
| TD-110 | SendGrid / Postmark provider switch (scale) | Medium | 2026-05-18 | Gmail App Password personal account caps at 500/day. First customer launch fine; second+ customer or campaign growth needs a transactional provider with API + webhook (bounces, complaints). Concrete: implement `ISendGridEmailService : IEmailService`, registered when `Email__Provider="SendGrid"`. Keep Gmail provider for dev. Cited in `appsettings.json` Email section + `GmailSmtpEmailService` header | Phase 30A v1. Gmail works for v3.0.0 |
| TD-111 | Health-check timeout + concurrency tuning | Low | 2026-05-18 | `MasterDbHealthCheck` uses 5s command timeout; under load the readiness probe might queue. Add `Microsoft.Extensions.Diagnostics.HealthChecks` `Timeout(2s)` decorator + per-check concurrency cap. Pair with TD-073 perf baseline so we know what numbers to set | Phase 30A v1. Single-tenant load fine without tuning |
| TD-112 | `Test-Local-Deploy.ps1` could create `WMS_Master` DB if missing | Low | 2026-05-18 | Current behaviour: script trusts `WMS_Master` exists (per `feedback_no_auto_create_db.md`). Local-dev convenience: detect missing DB + offer to `CREATE DATABASE WMS_Master` with confirmation prompt. Phase 30B production never auto-creates | Phase 30A v1. Manual SSMS step is fine for now |
| TD-113 | Smoke runner — auth-required scenario coverage | Low | 2026-05-18 | `Smoke-Local.ps1` covers 12 anonymous-surface scenarios. Auth-required surfaces (logged-in /SuperAdmin/Dashboard render, /Auth/SelectTenant after auth) need session cookie threading + scripted login. Approach: separate `Smoke-Authenticated.ps1` that calls `/SuperAdmin/Auth/Login` with seeded creds + captures cookie. Useful for CI smoke gate but adds credential management complexity | Phase 30A v1. Manual S1-S12 checklist covers it today |
| TD-114 | Phase 30B real-server deployment automation | Medium | 2026-05-18 | Today: deployment is manual via runbook + `Test-Local-Deploy.ps1` (workstation-only). Phase 30B = first real server. Automation options: PowerShell DSC scripts / Octopus Deploy / Ansible / Azure Pipelines deployment task. Decision deferred until hosting model + customer count clarifies. Pair with TD-067 CI/CD pipeline | Phase 30A v1. Manual deploy fine for customer #1 |
| TD-116 | Login page validator UX upgrade — 4-state design | Low | 2026-05-12 | Current `/Auth/Login` shows red borders without specific error messages, no live email-format validation, submit always enabled, no show-password toggle, no lockout countdown. Phase 25 backend already has lockout + rate-limit + failed-attempt logic — just needs frontend exposure. 4 states proposed: (1) invalid format live with inline message; (2) auth failed banner with attempts-remaining-before-lockout; (3) account locked with live countdown + "contact admin" link + all inputs disabled; (4) submitting with spinner button + disabled inputs. Files: `Views/Auth/Login.cshtml`, new `wwwroot/js/auth-login.js`, AuthController returns structured error codes. ~4-6h | Phase 30A re-validation v1. UX polish for first-impressions on customer demos |
| TD-117 | Change-password validator UX — live rules + strength + match | Low | 2026-05-12 | Current change-password forms (`/Account/ChangePassword` + `/SuperAdmin/ChangePassword` + new `/Auth/ChangePassword` from P0 #4) show static rule lists with no live feedback. Proposed: live checklist that updates per-keystroke (8+ chars / mixed case / digit), strength bar (Weak/Strong with color), match indicator on confirm field, show-password toggle on all 3 fields, submit disabled until all rules pass + match. Reusable component: `wwwroot/js/password-validator.js` + `wwwroot/css/auth-validator.css` shared across all password forms (including future `/Auth/ResetPassword` from TD-056). Phase 25's PasswordPolicy.Validate is the backend source of truth — frontend mirrors the same rules. ~6-8h | Phase 30A re-validation v1. Build once, reuse on 3+ surfaces; same first-impressions motivation as TD-116 |

---

## Closed Items

| ID | Title | Priority | Discovered | Resolved | Commit | Notes |
|----|-------|----------|------------|----------|--------|-------|
| TD-001 | Audit field standardization | Medium | 2026-05-04 | 2026-05-04 | cf01fc3, 0c65fe3 | 10 tables (3 Master + 7 Tenant), 24 columns added, idempotent + roundtrip-tested |
| TD-002 | FK_Customers_Carrier orphan | Medium | 2026-05-04 | 2026-05-04 | d562cd3 | FK added by migration 027 with ON DELETE SET NULL after Carriers (021) landed |
| TD-003 | ProductCategories.Path / Name type mismatch | Low | 2026-05-05 | 2026-05-05 | e08dc40 | Migration 032 widens Path from VARCHAR(500) to NVARCHAR(500); IX_Categories_Path dropped + recreated around the ALTER |
| TD-010 | Activity tab on `/Products/Detail/{sku}` still showed hardcoded mock entries | Medium | 2026-05-08 | 2026-05-09 | c26998d, 5fd4d37 | Phase 6C: `MovementActivityMapper` (T1, c26998d) maps `StockMovement → ActivityItem`; `ProductsController.Detail` reads via `IStockMovementRepository.GetByProductAsync(productId, limit: 20)` (T2, 5fd4d37). Empty timelines for fresh products + DEMO-001 are the expected default — `_ActivityPanel` renders "No activity yet." in that case |
| TD-015 | `MovementActivityMapper` didn't resolve actor names or location codes | Low | 2026-05-09 | 2026-05-09 | bb2a114 | Phase 6D: SQL-JOIN strategy (chose over batch-lookup to match `ProductRepository`/`WarehouseRepository` JOIN-with-DTO pattern). New `StockMovementListRow` carries resolved `PerformedByName` + From/To location codes; `IStockMovementRepository.GetByProductAsync` LEFT JOINs `security.Users` (`COALESCE(FullName, Email, 'System')`) + `master.Locations × 2`. Mapper title now reads `"<b>Maya Rodriguez</b> received 5 units at WH-MAIN"`; actor + location HTML-encoded against XSS. Spawned TD-016 (putaway-pair grouping) |
| TD-013 | jQuery missing on `_AuthLayout` — `jquery.validate` scripts load without jQuery itself | Low | 2026-05-09 | 2026-05-09 | (Phase 6F) | Phase 6F: original framing under-counted the blast radius — the bug actually lives in `_ValidationScriptsPartial.cshtml` (referenced by Login *and* Receive + Putaway). Fix added `<script src="~/lib/jquery/dist/jquery.min.js">` before the validate libs in the partial; jQuery itself was already present at `wwwroot/lib/jquery/dist/`, just unreferenced. One-line fix, three pages cleared at once |
| TD-028 | Filter chip counts on list pages | Low | 2026-05-09 | 2026-05-10 | (Phase 10A) | Phase 10A T3: new `GetStatusCountsAsync` on both `IPurchaseOrderRepository` + `IReceivingHeaderRepository` runs a single `SUM(CASE WHEN Status=...)` aggregate sharing the rows query's WHERE filter EXCEPT for `@Status` (so the inactive chips still display per-status totals). Controllers append `counts` object to the `/Data` JSON envelope; views render `<span class="wmsp-chip-count">` next to each chip label using the existing Phase 8.5 picker styling (`wms-picker.css` already loaded on `_OfficeLayout` per Phase 9C hotfix `0356e2c`). Pinned by 1 PO + 2 Receiving controller tests. Note: `PurchaseOrderStatusCounts` + `ReceivingStatusCounts` records — Dapper binds positional ctors strictly, so `[All]` SQL alias preserved (reserved keyword) |
| TD-029 | PO Detail Lines tab missing | Medium | 2026-05-10 | 2026-05-10 | (Phase 10A) | Phase 10A T1: extended `_DetailLayout` with optional `CustomTabs` slot — additive, zero regression on Master Data Detail (empty list keeps the legacy 4-tab surface). New `PurchaseOrderLineRow` DTO carries Product code+name + UoM code resolved via INNER JOIN (`master.Products` + `master.UnitsOfMeasure`); same Phase 6D pattern as `StockMovementListRow`. Panel partial `_PoLinesPanel.cshtml` renders a 7-col table with mini fill-rate bar (color-coded green/amber/gray by ratio) and per-line status badge. Pinned by 3 controller tests on `Detail` |
| TD-030 | PO Detail Receipts tab missing | Medium | 2026-05-10 | 2026-05-10 | (Phase 10A) | Phase 10A T2: new `IReceivingHeaderRepository.GetReceiptsByPoIdAsync(poId)` returns `PoReceiptRow` (Id, ReceivingNumber, ReceivedAt, Status, LineCount, TotalReceivedQty) — distinct from `ReceivingActivityRow` because Receipts table needs the qty column the Activity feed shape doesn't carry. `_PoReceiptsPanel.cshtml` table click-throughs to `/Receiving/Detail/{number}`. Receipts tab declared after Lines in `CustomTabs` order. Note: PO Detail's Activity tab still feeds from `GetActivityByPoAsync` (chronological, with PerformedByName) — the two will diverge once status-change events flow into Activity |
| TD-022 | ReceivingHeaderService orchestration not wrapped in TransactionScope | Medium | 2026-05-09 | 2026-05-10 | (Phase 10B) | Phase 10B T3: wrapped the 4-step orchestration (create header+lines → per-line stock upsert → lot/pallet ref stamp → PO line bump → PO status auto-transitions) in `TransactionScope` with `TransactionScopeAsyncFlowOption.Enabled`. Trade-off: each repo gets its own connection from the factory, so the orchestration spans multiple physical SqlConnections within one scope — SqlClient's PSPE promotes the LTM transaction to MSDTC on the second connection enlist. Acceptable on Windows deployment (per architecture). The cleaner alternative (single shared connection threaded through every repo + sub-service) is a much larger refactor and was de-scoped. Repos `PurchaseOrderRepository.{CreateAsync, ReplaceLinesAsync}` + `ReceivingHeaderRepository.CreateAsync` detect `Transaction.Current != null` and skip their internal `BeginTransaction()` (which would otherwise throw "SqlConnection does not support parallel transactions"). Same pattern applies to the new `CancelReceivingAsync` (TD-023) |
| TD-023 | GR Cancellation / reversal flow not implemented | Medium | 2026-05-09 | 2026-05-10 | (Phase 10B) | Phase 10B T4-T8: full reverse-orchestration via `IReceivingHeaderService.CancelReceivingAsync(id, reason, userId)` wrapped in TransactionScope. Steps: (1) per line — subtract `ReceivedQuantity` from Stock at the 6-tuple key via existing `UpsertOnHandAsync(key, -qty, ctx)` with `MovementType=Adjust`, `ReferenceType='ReceivingLineCancellation'`, `ReferenceId=line.Id`; (2) decrement linked PO line's `ReceivedQuantity`; (3) flip header `Status='Cancelled'` + populate Cancelled* audit trio (migration 20260510_008 added `CancelledBy`/`CancelledAt`/`CancelReason`); (4) revert per-line PO status (server-side CASE on Received vs Expected); (5) revert PO header status via `IPurchaseOrderService.RevertStatusAfterCancelAsync` (Closed→Receiving/Open). Idempotent on already-Cancelled (returns false). `CK_Stock_OnHand_NonNegative` rejects underflow if operator already consumed the received stock — TX rolls back, banner explains "stock has been consumed". UI: `Cancel receipt` QuickAction enabled only on Posted; CSS-only `:target` modal at `_CancelReceiptModal.cshtml`; reason 3-500 chars (FluentValidation). Activity feed extended to include `ReferenceType='ReceivingLineCancellation'` movements alongside the original `ReceivingLine` ones |
| TD-078 | Email password to admin (SMTP) | Medium | 2026-05-15 | 2026-05-18 | (Phase 30A M1) | Phase 30A M1: `IEmailService` + `GmailSmtpEmailService` (System.Net.Mail with SYSLIB0014 suppressed — MailKit migration is TD-103) + `EmailTemplateRenderer` with embedded HTML+TXT templates for 4 types (Welcome / TempPassword / TenantCreated / PasswordReset). Wired into `TenantProvisioningService.CreateTenantAsync` (Step 6, post-audit) + `ResetTenantAdminPasswordAsync` (post-audit, OUTPUT clause captures admin email). Best-effort by design — SMTP failure logs Warning, provisioning + reset commits stand. `TestMode=true` safe default logs without sending. `EmailServiceHealthCheck` wired to `/healthz/ready` (Degraded / Unhealthy / Healthy). Open follow-ups: TD-103 MailKit / TD-104 retry queue / TD-105 audit log / TD-106 DKIM-SPF-DMARC / TD-109 Welcome email wiring on user create / TD-110 SendGrid for >500/day quota |
| TD-115 | Sidebar yellow dot duplicated on parent + leaf | Low | 2026-05-12 | 2026-05-12 | 91d3ef5 | Split overloaded `.active` into `.expanded` (parent w/ active child — subtle highlight + chevron rotation, NO yellow dot) + `.active` (leaf only — keeps the yellow dot). Markup: 7 parent blocks in `SidebarMenu/Default.cshtml` emit `.expanded` when a child route matches. CSS: new `.wms-sidebar-item.expanded` selectors + collapsed-mode variant. SuperAdmin nav unaffected (single-level top nav, no parent/child relationship). Drive-by: added `Putaway` to inboundExpanded check — was previously missing |

---

## Won't Fix

(empty)

---

## Process

### When you discover debt:
1. Add row to "Open Items" with next TD-NNN
2. Add comment in code: `// TODO(TD-XXX): summary`
3. Commit: `docs(tech-debt): add TD-XXX <title>`

### When you fix debt:
1. Move row from Open → Closed
2. Add commit hash
3. Update related code comments
4. Commit: `docs(tech-debt): close TD-XXX <title>`

### When deferring permanently:
1. Move to "Won't Fix"
2. Add reasoning in Notes
3. Commit: `docs(tech-debt): wontfix TD-XXX <title>`
